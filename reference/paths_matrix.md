# Simulated Travel Path with multiple IDs

matrix created with travelpaths::sim_travel_paths().

## Usage

``` r
paths_matrix
```

## Format

An object of class `matrix` (inherits from `array`) with 3000 rows and 4
columns.
